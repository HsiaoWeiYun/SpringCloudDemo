server:
  port: 9090
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false   #不向註冊中心註冊自己
    fetch-registry: false   #不需要檢索服務
    service-url:
      defaultZone: http://peer2:9091/eureka